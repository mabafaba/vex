{
  "name": "vex",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker": "docker build -t vex . && docker compose up",
    "deploy": "git pull && docker build -t vex . && docker compose up",
    "dev": "nodemon server.js",
    "watch": "browser-sync start --proxy 'localhost:3000' --watch true"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "fs": "^0.0.1-security",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.13.1",
    "path": "^0.12.7",
    "sanitize-html": "^2.15.0",
    "socket.io": "^4.8.1"
  }
}
